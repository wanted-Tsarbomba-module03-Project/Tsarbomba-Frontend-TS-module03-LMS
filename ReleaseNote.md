# v1.6.4 (2026-07-08)

### 문제풀이 방 힌트/해설 조회 보정

- **Fixed**
  - 문제풀이 방에서 이미 오답/정답 상태로 진입한 소문제의 힌트 탭이 활성화되어도 실제 힌트 목록을 조회하지 않아 "힌트가 없습니다"로 표시되던 문제 수정
  - 힌트 조회 API가 문제 세트 ID가 아닌 소문제 `problemId` 기준(`/api/v1/problems/{problemId}/hints`)으로 호출되도록 탭 진입 시점 조회 흐름 보강
  - 강의 문제풀이 화면에서도 백엔드가 해설을 `solution` 필드로 내려주는 경우 `explanation`만 참조해 "해설이 없습니다"로 표시될 수 있던 문제 수정
- **Changed**
  - 힌트 응답을 `hintOrder` 오름차순으로 정렬해 단계별 힌트 노출 순서를 보장
  - 문제 상세 정규화에서 해설 필드를 `explanation ?? solution` 기준으로 통합 처리

### 문제풀이 코드 입력 UI 개선

- **Added**
  - 문제풀이/강좌 문제풀이 코드 입력창에 Monaco Editor 적용
  - Monaco 입력창 우측 상단에 다크/라이트 테마 토글 추가
- **Changed**
  - 코드 입력창을 코드블록 형태로 명확하게 표시하고 사용자가 세로 높이를 조절할 수 있도록 개선
  - `Tab` 입력은 코드 들여쓰기로 동작하도록 유지하고 기본 들여쓰기 폭을 4칸으로 조정
  - Monaco 자동완성/스니펫/파라미터 힌트 제안 UI 비활성화
  - 실행결과/추천강좌/힌트/해설보기 탭 버튼의 기본/활성/비활성 색상 대비를 강화하고 기본 배경색을 `#fff`로 조정
  - 문제풀이 상세 스켈레톤 UI를 Monaco 입력창과 탭 상태 색상 구조에 맞게 보정
- **Fixed**
  - Monaco Editor 청크 로드 실패 시 무한 로딩 대신 사용자 안내 fallback이 표시되도록 에러 바운더리 추가

---

# v1.6.3 (2026-07-02)

### 강의 생성/수정 problemCategoryId 400 수정

- **Fixed**
  - 강의 생성/수정 요청 페이로드에 `problemCategoryId` 필드 추가 — 배포 BE의 `LectureCreateRequest`가 `problemCategoryId`를 `@NotNull` 필수 필드로 요구해 400이 반환되던 문제 해결
- **Changed**
  - `LectureUpsertBody` 타입에 `problemCategoryId: number` 추가 (선택 → 필수)
  - 강좌 등록/수정 폼 validate에 "강의가 하나라도 있으면 문제 카테고리 선택 필수" 검증 추가 — 미선택 상태로 등록 시도 시 안내 모달로 사전 차단

---

# v1.6.2 (2026-07-02)

### GCS 이미지 화이트리스트 및 강의 lectureType 400 수정

- **Fixed**
  - `next.config.ts` `imageRemotePatterns`에 `https://storage.googleapis.com/codebombalms` 추가 — 프로덕션 강좌 썸네일이 `next/image`에서 400으로 거부되던 문제 해결 (rel-ys 후속)
  - 강의 생성/수정 요청 페이로드에서 `lectureType` 필드 제거 — BE `LectureCreateRequest` record에 없는 필드로 인해 Jackson unknown property 400이 반환되던 문제 해결
- **Changed**
  - `LectureSummary`, `LectureUpsertBody` 타입에서 `lectureType` 필드 제거
  - 영상/문제 강의 구분은 기존대로 `videoUrl` 유무 기준 유지 (내부 로직 변화 없음)

---

# v1.6.1 (2026-07-02)

### 폰트 및 tsar-dog 이미지 최적화

- **Changed**
  - `PretendardVariable.woff2`를 KS X 1001 상용한글 2350자 기준으로 서브셋 (2MB → 491KB, 약 75% 감량)
  - `@font-face` 에 `font-weight: 45 920` (variable font 범위) 및 `font-display: swap` 명시 — FOIT 제거로 폰트 로딩 체감 개선
  - `public/assets/img/tsar-dog/` 마스코트 SVG 12개(basic / error / tsardog_400~502)의 임베드 초고해상도 PNG(3966×4096)를 표시 크기에 맞게 리사이즈 후 WebP로 재임베드 (파일명·구조 유지, 폴더 43MB → 약 800KB)
- **Removed**
  - 사용되지 않던 tsar-dog SVG 4개 정리 (`angry_tsardog.svg`, `happy_tsardog.svg`, `run_tsardog.svg`, `typing_tsardog.svg`)
- **Performance**
  - Slow 4G 기준 홈/강좌/404 페이지 전송량 6.8~10.3MB → 800~910KB (약 87~91% 감량)
  - Slow 4G 홈 Load 시간 약 40s → 6s
  - Lighthouse Performance 80 → 91, SEO 82 → 100, Accessibility +4~5, LCP 2.3s (Core Web Vitals 통과)

---

### SEO 및 구조화 데이터

- **Added**
  - 문제풀이 목록, 문제 상세, 랭킹 페이지에 Schema.org JSON-LD(`BreadcrumbList`, `ItemList`) 추가
  - `robots.txt` 추가 및 `/admin`, `/api` 경로 크롤링 차단 처리
  - `sitemap.xml` 추가 및 공개 검색 대상 라우트 명시
- **Changed**
  - `(user)` 라우트 그룹에 기본 `metadataBase`, title template, authors/publisher, 기본 Open Graph/Twitter 메타 추가
  - 문제풀이 목록/상세, 랭킹, 챗봇, 에러, 관리자 주요 화면에 title, description, canonical, Open Graph, Twitter, robots meta 적용
  - 개인화/운영 성격의 챗봇, 에러, 관리자 화면은 `noindex`로 검색 노출 대상에서 제외
  - 문제 목록 row, 문제 카테고리 사이드바, 관리자 뱃지/강의 카드 이동을 실제 anchor 기반 `Link`로 보완
  - 챗봇 화면 제목을 `h1`로 조정하고 로딩/관리자 화면의 semantic container 구조 보완
- **Fixed**
  - `next.config.ts`의 `reactStrictMode: false` 설정 제거
  - 원격 이미지 허용 범위를 전체 도메인(`hostname: "**"`)에서 https 기반 allowlist(`NEXT_IMAGE_REMOTE_PATTERNS`, `NEXT_PUBLIC_API_URL`, `API_PROXY_TARGET`)로 제한
  - 공통 이미지 최적화 유틸에서 `loading: "lazy"` 고정을 제거해 호출부별 로딩 전략 선택 가능성을 보장
  - 카드형 이동 UI의 `div onClick` 사용을 줄여 키보드/스크린리더 접근성 보완

---

### Docker 이미지 최적화

- **Changed**
  - Next Image 원격 이미지 최적화 설정을 추가하고 AVIF/WebP 포맷 출력을 활성화
  - 공통 blur placeholder와 lazy loading props를 추가해 이미지 로딩 체감 성능을 개선
  - 헤더/사이드바 장착 뱃지, 관리자 뱃지, 랭킹 뱃지, 강좌/마이페이지 썸네일 이미지를 Next.js `Image` 컴포넌트 기반으로 정리
  - 문제풀이 결과 패널의 추천 강좌 썸네일을 CSS `backgroundImage` 방식에서 Next.js `Image` 컴포넌트 기반 렌더링으로 변경
  - 원격 이미지의 직접 loader/unoptimized 처리를 제거해 Next 이미지 최적화 파이프라인을 사용하도록 변경
  - 관리자 강좌 카드의 외부 placeholder 이미지 직접 로드를 제거하고 로컬 fallback 이미지를 사용하도록 변경

- **Fixed**
  - 직접 `<img>` 사용 지점을 제거하고 반응형 `sizes`, lazy loading, blur placeholder를 적용해 고해상도 원본 이미지 직접 로드 가능성을 완화
  - CSS `backgroundImage`로 원본 썸네일을 직접 로드하던 지점을 제거해 이미지 최적화 누락 가능성을 완화
  - 로컬 파일 미리보기 이미지는 blob/data URL 호환성을 위해 `Image` 컴포넌트를 유지하되 최적화 예외 처리

---

# v1.6.0 (2026-07-01)

### 반응형 레이아웃 및 모바일 사이드바

- **Changed**
  - 모달, 로딩, 에러, 목록, 페이지네이션 공통 UI가 모바일 화면에서 영역을 넘치지 않도록 폭/높이/스크롤 동작을 보완
  - 관리자 뱃지/알람/규칙/마스터, 문제풀이 목록/상세, 랭킹, 범용/문제풀이 챗봇 화면의 모바일 레이아웃을 반응형으로 조정
  - 1023px 이하에서 숨겨지는 사이드바를 `sidebar.svg` 버튼으로 열고 닫을 수 있도록 개선하고, 사이드바를 화면 중앙 오버레이 형태로 표시
  - 문제풀이 목록/상세의 모바일 사이드바 외부 클릭 닫힘, 카테고리/문제 이동 시 자동 닫힘, 문제풀이 챗봇 열림 시 사이드바 버튼 비가시화 처리

---

### 강좌·강의 UX

- **Added**
  - 강좌 문제풀이 화면에 데이터셋(CSV) 다운로드 버튼 추가 (문제풀이방과 동일 — `POST /problem-sets/{id}/dataset/download-url`)

- **Changed**
  - 강좌 등록/수정 폼에서 한 강좌엔 하나의 문제 카테고리만 배정하도록 잠금 — 문제 배정 후 카테고리 변경 시 안내 (FINAL 추천 기준 일관성)
  - 운영/관리 역할은 미수강·순서 잠금 관계없이 강의 열람 가능하도록 가드 예외 처리

- **Fixed**
  - 강좌 등록/수정 폼의 등록·취소·문제 선택 드롭다운 등 다수 버튼에 마우스 커서(hover) 누락 보정

---

### 문제풀이 목록 검색

- **Added**
  - 회원 문제풀이 목록(`/problems`)과 관리자 문제 관리 목록(`/admin/problems`)에 기존 공통 `Searchbar` 기반 검색 UI 추가
  - 문제 제목 기준 검색을 지원하고, 검색 결과에서도 목록 페이지네이션이 동작하도록 처리
  - 회원 문제풀이 목록 검색 시 현재 선택된 카테고리 범위가 유지되도록 `categoryId`를 검색 조회에 전달

---

### 마이페이지 뱃지 동기화

- **Added**
  - 뱃지 선택 모달에 "동기화" 버튼 추가 — `POST /badges/me/sync`로 서버에서 뱃지를 재계산한 뒤 목록/대표 뱃지를 갱신 (자동 등록 누락 대비 수동 동기화)

---

### 공통 목록 UX

- **Changed**
  - 공통 `List` 셀의 기본 표시 방식을 한 줄 말줄임으로 정리하고, 문제풀이 목록의 문제명/문제 설명은 최대 2줄까지 표시되도록 조정
  - 셀 내용이 실제로 잘린 경우(`scrollWidth > clientWidth` 또는 `scrollHeight > clientHeight`)에만 호버 시 전체 내용을 확인할 수 있도록 개선
  - 번호 컬럼은 명시 플래그로 판별해 호버 전체 내용 표시 대상에서 제외하고, `clamp(48px, 6ch, 72px)` 폭을 적용해 나머지 셀이 더 많은 영역을 사용할 수 있도록 조정

---

### 마이페이지 소셜 계정 분기

- **Changed**
  - `GET /me`의 `provider`(LOCAL/GOOGLE) 기준으로 마이페이지 흐름 분기
  - 소셜(구글) 계정 정보 수정 시 비밀번호 재인증(verify-password)을 건너뛰고 바로 편집 → `PUT /me`
  - 소셜 계정은 비밀번호가 없어 "비밀번호 변경" 항목을 숨김
  - 회원 탈퇴: 소셜 계정은 확인 문구("탈퇴하겠습니다") 입력 UI + `{ confirmText }` 전송, 일반 계정은 기존 `{ password }` 유지

---

### 범용 챗봇

- **Fixed**
  - 새 대화 생성 후 `/chat/{roomId}`로 전환될 때 라우터 재렌더로 채팅 화면이 깜빡이던 문제 수정
  - 새 채팅방 생성 시 메시지 화면은 유지하면서 내부 roomId와 브라우저 URL만 동기화하도록 변경
  - 새로 생성된 채팅방 제목이 즉시 반영되지 않던 문제 수정 — 채팅방 메타데이터를 백그라운드로 조회해 제목/연결 문제 정보를 갱신
  - 채팅방 목록 갱신 이벤트에서 기존 목록을 유지하고 백그라운드로 재조회하도록 변경해 사이드바 깜빡임 완화

### 목록/페이지네이션

- **Fixed**
  - 목록 페이지의 `loading.tsx`가 상세/등록/수정 하위 라우트에도 적용되어 리스트를 사용하지 않는 화면에서 목록 스켈레톤이 보이던 문제 수정 — 목록 페이지를 `(list)` route group으로 분리해 로딩 범위 제한
  - 페이지네이션이 있는 표의 `No.` 컬럼이 페이지마다 1부터 다시 시작하던 문제 수정 — 공통 `List`에 `rowNumberOffset`을 추가해 2페이지부터 `21-40`처럼 누적 표시
  - 관리자 회원/알람/관리자 계정/강좌 학습 현황 목록의 페이지별 번호 표시를 실제 페이지 위치 기준으로 보정

---

# v1.5.1 (2026-07-01)

### 강좌 접근 가드

- **Fixed**
  - 미수강·앞 강의 미완료 상태로 강의/문제에 접근 시 배포 환경에서 안내 없이 404로 빠지던 문제 수정 — 영상 강의(`getLecture`)·문제(`getLectureProblemSet`)가 403/404를 반환하면 죽은 에러 화면 대신 강좌 페이지로 리다이렉트

---

### 헤더

- **Fixed**
  - 넓은 화면에서 검색바가 로고·네비 텍스트를 가리고 화면 축소 시 텍스트가 깨지던 문제 수정 — 로고·네비 `shrink-0`/`whitespace-nowrap`, 검색바 `min-w-0` 로 축소 우선순위 정리

- **Changed**
  - 검색바를 홈(강좌 목록)에서만 노출하도록 변경 + 좁은 화면(md 이하)에서 네비 메뉴 숨김

---

### 구글 로그인

- **Fixed**
  - 일반 이메일로 가입된 계정으로 구글 로그인 시 안내 없이 URL에 에러(`?error=AUT-022`)만 남던 문제 수정 — 로그인 페이지에서 에러 코드를 읽어 안내 모달 표시 + URL 정리

---

### 영상 강의 수강

- **Fixed**
  - 재생바를 끝까지 드래그하면 실제 시청 없이 완료 처리되던 문제 수정 (재생 중·종료 모두 실제 시청 위치 기준으로 판정)
  - 완료한 영상 재진입 시 끝 위치에서 시작해 검은 화면이 뜨던 문제 → 처음부터 재생
  - 플레이어 준비(onReady) 전 진도 저장 호출 시 `getDuration` 런타임 오류가 나던 문제 가드

---

### 강좌 문제풀이

- **Added**
  - 강좌 문제풀이 화면에 문제 내용 / 풀이 영역 좌우 드래그 리사이즈 추가 (문제풀이방과 동일 동작, 공유 훅)

- **Fixed**
  - 배포 환경에서 강좌 문제 페이지 진입 시 서버 컴포넌트 상대경로 fetch 실패(`Failed to parse URL`) 수정 — `API_PROXY_TARGET` 절대주소 폴백
  - 패널 드래그 도중 페이지 이동(언마운트) 시 전역 커서(col-resize) 잠김 / 포인터 리스너 누수 정리

---

### 강좌 상세

- **Changed**
  - 추가 문제 버튼을 수강 중일 때만 노출하도록 변경 + 추가 문제·이전/다음·문제 풀러 가기 버튼 hover/커서 보정

---

### 챗봇 UI/UX

- **Changed**
  - 범용 챗봇 채팅창 세로 영역 확대
  - 채팅창 내부 스크롤바를 표시해 스크롤 가능 여부가 보이도록 변경
  - 범용 챗봇 페이지 전체 스크롤바는 숨기되 스크롤 동작은 유지하도록 변경

- **Fixed**
  - 챗봇 응답 생성 중 사용자가 위로 스크롤하면 자동으로 다시 하단으로 끌려가던 문제 수정
  - 하단에 머무른 상태에서 새 답변이 생성될 때 스크롤이 답변 끝까지 안정적으로 따라가지 않던 문제 수정

---

### 공통 목록 스켈레톤 UI

- **Added**
  - 공통 `ListSkeleton` 컴포넌트 추가 — `List` 기반 화면에서 제목/테이블 헤더/row/페이지네이션 형태의 로딩 UI 제공
  - 랭킹, 관리자 문제/회원/알람/관리자 계정/강의 목록 라우트에 최초 로딩 스켈레톤 추가
  - 내 강의실(`/myclassroom`)에 진행 중/완료 강의 카드형 스켈레톤 추가
  - 범용 챗봇(`/chat`, `/chat/[roomId]`)에 채팅방 목록 및 메시지 말풍선 스켈레톤 추가
  - 관리자 회원 상세(`/admin/users/[id]`)에 프로필 정보 블록/탭/목록 스켈레톤 추가
  - 관리자 문제 수정(`/admin/problems/[id]/edit`)에 문제 정보/파일/소문제/테스트케이스 폼 스켈레톤 추가

- **Changed**
  - 관리자 문제/회원/알람/관리자 계정 목록, 랭킹 전환, 회원 상세 탭 목록, 강좌 상세 학습 현황 모달의 목록 로딩 상태를 테이블형 스켈레톤으로 변경
  - 스켈레톤 표/페이지네이션은 보조기기에서 숨기고 `role="status"` 기반 로딩 메시지만 읽히도록 접근성 처리
  - 목록 스켈레톤 컬럼 라벨과 페이지 크기를 공용 상수로 분리해 실제 테이블 컬럼과 로딩 UI가 함께 변경되도록 정리
  - 공통 `ListSkeleton`의 React key 생성 방식과 컬럼별 skeleton 폭 계산을 개선해 중복 라벨/다양한 컬럼 수에 안전하게 대응

---

# v1.5.0 (2026-06-30)

### 마이페이지 보안

- **Added**
  - 마이페이지 "보안" 메뉴 + 페이지(`/user/profile/security`) 추가
  - 로그인 이력 조회 — 시간 / 위치(국가·도시·IP) / 의심여부 표시 (`GET /me/login-history` 연동)
  - 신뢰 기기 목록 조회 — 현재 기기 표시 포함 (`GET /me/trusted-devices` 연동)
  - 신뢰 기기 해제 — 확인 모달 후 해제, 다음 로그인 시 추가 인증 재요구 (`DELETE /me/trusted-devices/{deviceId}` 연동)
  - 각 카드 화면 높이 제한 + 목록 내부 스크롤, 로딩/실패/빈 목록 상태 처리

---

# v1.4.0 (2026-06-30)

### 강좌 추천 문제

- **Added**
  - 강좌 상세 수강 신청 버튼 옆 `추가 문제` 버튼 + 추천 모달 추가 — 강좌의 문제 카테고리에서 강좌 미포함 문제세트를 랜덤 추천 (`GET /lectures/{lectureId}/final-problem-set-candidates` 연동)
  - 모든 강의를 수강하기 전 누르면 "모든 강의를 수강한 뒤 이용" 안내 표시 (BE 403 검증 기준)

- **Fixed**
  - 강좌 등록 시 문제 카테고리 선택 후 문제세트가 페이지네이션 응답(`data.content`)으로 바뀌어 안 뜨던 문제 수정

---

### 랭킹 페이지

- **Fixed**
  - 배포 환경에서 랭킹 페이지 첫 진입 시 서버 렌더링 단계의 내 랭킹 정보 조회가 실패하던 문제 수정
  - 랭킹 API 요청 시 서버 런타임에서는 `API_PROXY_TARGET` 또는 `NEXT_PUBLIC_API_URL` 기반 절대 URL을 사용하도록 보완

---

# v1.3.1 (2026-06-30)

### AI 채팅 Typewriter (#187)

- **Fixed**
  - 챗봇 typewriter 유틸의 `timerId` 타입을 `ReturnType<typeof setTimeout>` 기준으로 수정해 브라우저/Node 타이머 타입 불일치로 발생하던 빌드 오류 해결
  - `window.setTimeout` / `window.clearTimeout` 호출을 `setTimeout` / `clearTimeout`으로 통일

---

# v0.3.0 (2026-06-30)

### AI 채팅 스트리밍 (#176, #182)

- **Added**
  - 일반 채팅 및 문제 풀이 채팅에 SSE 기반 스트리밍 응답 처리 추가
  - 스트림 이벤트(`room`, `done`, `error`, token)를 처리하는 공통 `streamChat` 유틸 추가
  - 챗봇 응답 토큰을 내부 버퍼에 쌓고 한 글자씩 표시하는 공통 typewriter 유틸 추가

- **Changed**
  - 메시지 전송 후 응답 전체를 기다리던 방식에서 토큰 단위로 답변을 표시하도록 변경
  - 신규 채팅방 생성 시 스트림에서 전달된 roomId 기준으로 채팅방 이동/상태 갱신 처리
  - 백엔드 토큰 수신 속도와 별개로 프론트에서 일정한 간격으로 답변 글자를 표시하도록 변경
  - 첫 토큰 수신 시 `AI 응답 중입니다.` 패널을 제거하고 답변 표시 상태로 전환

- **Fixed**
  - 스트림 중단 또는 네트워크 오류 시 기존 오류 모달과 에러 메시지로 실패 상태를 안내하도록 처리
  - 빈 assistant 메시지가 채팅 목록에 노출되지 않도록 렌더링 처리 보완
  - 스트리밍 답변 표시 중 채팅 화면이 깜빡이는 것처럼 보이던 문제 완화
  - 문제풀이방 새 채팅방 생성 시 채팅 내역은 유지하면서 채팅방 제목이 바로 갱신되도록 수정

---

### 영상 강의 수강

- **Fixed**
  - 영상 강의 재진입 시 마지막으로 본 위치보다 뒤로 되감기던 문제 수정 — 이어보기 위치를 `Math.min(lastPositionSec, watchedSec)` 에서 `lastPositionSec`(영상 위치) 기준으로 변경 (앞 구간 스킵·배속 차단은 기존 방어 로직 그대로 유지)

---

### 강좌 학습 현황

- **Fixed**
  - 수강생 학습 현황 페이지네이션(#162) 적용 후 `getCourseLearningProgress` 반환 타입 변경으로 `CourseDetailClient` 에서 발생하던 배포 빌드 타입 오류 수정

---

# v0.2.0 (2026-06-29)

## 구글 소셜 로그인 (#167)

- **Added**
  - 구글 OAuth2 로그인 연동 — 동의 URL 이동 / 임시정보 조회 / 가입 완료
  - 신규 가입 추가정보 페이지 (닉네임 중복확인 + 전화번호 검증)

- **Fixed**
  - 기존 회원 구글 재로그인 시 헤더에 닉네임이 안 뜨던 문제 (프로필 1회 조회로 복구)

---

## 마이페이지 뱃지 (#157)

- **Added**
  - 사이드바 프로필 클릭 → 보유 뱃지 중 대표 뱃지 선택/장착
  - 장착한 뱃지가 사이드바·헤더 닉네임 옆에 실시간 반영

---

### 관리자 뱃지 관리 (#155, #159)

- **Added**
  - 뱃지 관리 페이지 — 뱃지 조회/등록
  - 뱃지 수정/삭제 기능

---

### 수강생 학습 현황 (#162)

- **Added**
  - 학습 현황 모달 서버사이드 페이지네이션
  - 수강생별 문제 풀이 현황(정답/오답/미제출) 조회

---

### 강좌·영상 강의 개선 (#N)

- **Changed**
  - 영상 강의 완료 기준을 끝까지 시청으로 강화 (재생 중 89% 상한, 종료 시 100%)
  - 첫 시청 중 배속 재생 차단 + 안내 문구 갱신

- **Fixed**
  - 강좌 등록/수정 버튼 연타 시 강좌가 여러 개 생성되던 문제 (동기 ref 가드)
  - 영상 완료 모달이 늦게/안 뜨던 문제 (재생바 끝 도달 즉시 트리거)

### 관리자 뱃지 관리

- **Added**
  - 관리자 뱃지 목록 조회 페이지 추가
  - 관리자 뱃지 등록 페이지 추가
  - 관리자 뱃지 수정 및 삭제 페이지 추가
  - 뱃지 이미지 업로드 및 미리보기 기능 추가
  - 뱃지 상태(`ACTIVE`, `INACTIVE`) 수정 기능 추가
  - 뱃지 등록·수정·삭제 확인 모달 추가
  - `/api/v1/admin/badges` 조회 및 등록 API 연동
  - `/api/v1/admin/badges/{badgeId}` 상세 조회, 수정, 삭제 API 연동

- **Changed**
  - 뱃지 목록을 카드형 UI로 구성하여 이미지, 이름, 설명, 필요 포인트, 상태, 등록일을 표시
  - 뱃지 등록·수정 화면을 강좌 등록 페이지와 유사한 폼 UI로 구성
  - 뱃지 목록 카드 클릭 시 수정 페이지로 이동하도록 변경
  - `/admin/badges/new`, `/admin/badges/[id]/edit` 진입 시 사이드바 뱃지 관리 메뉴 활성화

- **Fixed**
  - 뱃지 상세 조회 실패 또는 정보 없음 상태에서 안내 모달이 표시되지 않을 수 있는 문제 수정
  - 뱃지 수정 화면 모달 상태에서 성공 여부와 목록 이동 여부가 섞이던 상태값 분리

---

### 문제 풀이 페이지

- **Fixed**
  - 배포 환경에서 서버 컴포넌트의 상대경로 fetch로 `/api/v1/problem-categories` 호출이 실패하던 문제 수정
  - 문제 도메인 API 요청 시 서버 런타임에서는 `API_PROXY_TARGET` 또는 `NEXT_PUBLIC_API_URL` 기반 절대 URL을 사용하도록 보완
  - 서버 API base URL 누락 시 URL 파싱 오류 대신 설정 누락 오류가 명확히 발생하도록 수정

---

# v0.1.1 (2026-06-28)

### 기능

- **Added**
  - 없음

- **Changed**
  - 없음

- **Fixed**
  - 어드민 operations API `NEXT_PUBLIC_API_URL` 빈값 시 500 오류 / 흰 화면 핫픽스 (#152)

---

# v0.1.0 (2026-06-26)

### 기능

- **Added**
  - 로그인 추가 인증(step-up) — 미신뢰 기기 로그인 시 이메일 OTP 인증 + "이 기기 신뢰" 옵션 (#132)
  - 이메일(아이디) 찾기 — 이름 + 전화번호로 마스킹된 이메일 조회 (#140)
  - 강의자료 첨부 — 다중 파일 업로드 / 목록 / 다운로드 (#146)
  - 강좌 접근 가드 — 미수강 강좌 강의·문제 URL 직접 접근 차단, 잠금 순서 위반 시 이동 (#121)
  - 마이페이지 비밀번호 변경 — 비밀번호 확인 없이 바로 변경 폼 진입 (#117)
  - 최고관리자(master admin) 페이지 생성 (#115, #119)
  - 관리자 회원관리 페이지네이션 적용 (#104)
  - 문제풀이 페이지 문제 추천 모달 (#126)
  - 문제풀이 페이지 추천 강좌 기능 (#138)
  - 문제풀이 페이지 스켈레톤 UI (#130)
  - 문제풀이 페이지 사용자 편의 및 UI 개선 (#124)
  - 에러페이지 차르독 이미지 추가 (#144)
  - 마스코트 이미지 추가 (#142)

- **Changed**
  - 마이페이지 비밀번호 변경 UX 개편 — 이메일 OTP 제거, `PUT /me/password` 직접 연동
  - 로그인 API 프록시 설정 + 서버 컴포넌트 fetch 절대경로 수정 (#128)
  - `NEXT_PUBLIC_API_URL` 빈값 시 throw 제거 → `?? ""` 폴백으로 전환 (#136)

- **Fixed**
  - 강의 진도/완료 처리 — 문제 강의 구분, 완료 모달, 문제세트 경로 정정 (#134)
  - Docker 빌드 prerender 실패 — `useSearchParams` Suspense 래핑 (#111)

---
